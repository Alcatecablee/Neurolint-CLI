# Layer 5 Next.js & React 19 Migration - Verified Facts for X Posts

> **IMPORTANT**: This document contains verified facts extracted from the actual codebase.
> Every claim can be backed up with specific file paths and line numbers.
> Last verified: December 11, 2025

---

## Quick Reference: Verified Numbers

| Claim | Verified Value | Source File |
|-------|---------------|-------------|
| Total Lines of Code | 2804 | `wc -l scripts/fix-layer-5-nextjs.js` |
| React 19 DOM API Transformations | 5 | `fix-layer-5-nextjs.js` lines 33-331 |
| Next.js 15.5 Deprecation Patterns | 15 | `NextJS15DeprecationHandler` lines 2282-2402 |
| ESLint to Biome Rule Mappings | 7 | `BiomeMigrationTransformer` lines 1996-2004 |
| Transformer Classes | 4 | Lines 438, 702, 1994, 2280 |
| Exported Functions | 6 | `module.exports` lines 2795-2805 |

---

## Verified Claim #1: "React 19 DOM API Auto-Migration"

### TRUE - But NOT Unique (Similar to react-codemod)

**Actual Implementation:**

Layer 5 handles 5 specific React 19 breaking changes using **manual parenthesis parsing** (not pure regex):

```javascript
// Line 33-131: convertReactDOMRender()
function convertReactDOMRender(code) {
  let transformedCode = code;
  const changes = [];
  let rootCounter = 0;
  
  const renderRegex = /ReactDOM\.render\s*\(/g;
  // Manual parsing to find matching parentheses
  // Handles nested JSX and function calls correctly
  
  // Generate unique root variable names
  const rootVarName = rootCounter === 0 ? 'root' : `root${rootCounter}`;
  rootCounter++;
  
  const replacement = `const ${rootVarName} = createRoot(${container});\n${rootVarName}.render(${jsxElement});`;
}
```

| Change Type | Function | Lines | What It Does |
|-------------|----------|-------|--------------|
| `ReactDOM.render` | `convertReactDOMRender()` | 33-131 | Converts to createRoot().render() |
| `ReactDOM.hydrate` | `convertReactDOMHydrate()` | 137-231 | Converts to hydrateRoot() |
| `unmountComponentAtNode` | `convertUnmountComponentAtNode()` | 237-261 | Adds warning comment |
| `findDOMNode` | `detectFindDOMNodeUsage()` | 267-293 | Adds deprecation warning |
| `react-dom/test-utils` | `convertReactDOMTestUtils()` | 299-331 | Moves act to 'react' package |

### Competitive Landscape (Web Research - December 2024):

| Tool | ReactDOM.render → createRoot | hydrate → hydrateRoot | act migration | Source |
|------|------------------------------|----------------------|---------------|--------|
| **react-codemod (official)** | Yes | Yes | Yes | `npx codemod@latest react/19/replace-reactdom-render` |
| **types-react-codemod** | No (types only) | No | No | `npx types-react-codemod@latest preset-19` |
| **ast-grep** | Manual patterns | Manual | Manual | Community tool |
| **NeuroLint Layer 5** | Yes | Yes | Yes | `fix-layer-5-nextjs.js` |

**Official react-codemod command:**
```bash
npx codemod@latest react/19/replace-reactdom-render
```

### What Makes Layer 5 POTENTIALLY Different:

1. **Unique root variable naming** (`root`, `root1`, `root2`) - Line 42 in fix-layer-5-nextjs.js
   - Need to verify if react-codemod handles multiple render calls the same way
   
2. **Centralized backup integration** - Uses BackupManager before transformations

3. **Combined with Next.js 15.5 patterns** - One tool for both React 19 + Next.js

### How to Accurately Describe This:

**CORRECT:** "Layer 5 handles 5 React 19 DOM API migrations similar to react-codemod"

**CORRECT:** "Layer 5 generates unique variable names (root, root1, root2) for multiple render calls"

**CORRECT:** "Layer 5 integrates React 19 migration with Next.js 15.5 patterns in one tool"

**INCORRECT:** "No other tool does this" (react-codemod exists and is official)

**INCORRECT:** "First React 19 migration tool" (react-codemod is official and predates Layer 5)

### Limitations:

- Uses manual parenthesis matching, not full AST parsing (react-codemod uses jscodeshift AST)
- Similar functionality exists in official react-codemod
- Only handles ReactDOM API changes, not all React 19 breaking changes

---

## Verified Claim #2: "15 Next.js 15.5 Deprecation Patterns"

### TRUE - Overlap with @next/codemod

**Actual Implementation (lines 2282-2402):**

```javascript
class NextJS15DeprecationHandler {
  constructor() {
    this.deprecationPatterns = {
      legacyBehavior: {
        pattern: /legacyBehavior\s*=\s*{?[^}]*}?/g,
        replacement: '',
        autoFix: true,
        description: 'Remove legacyBehavior prop from Link components'
      },
      nextLint: {
        pattern: /"next lint"|"next lint .*?"|'next lint'|'next lint .*?'/g,
        replacement: '"biome lint ./src"',
        autoFix: true,
        description: 'Replace "next lint" with Biome (deprecated in Next.js 15.5)'
      },
      // ... 13 more patterns
    };
  }
}
```

**Full Pattern Breakdown:**

| # | Pattern Name | Auto-Fix | Description | Lines |
|---|--------------|----------|-------------|-------|
| 1 | `legacyBehavior` | Yes | Remove deprecated Link prop | 2283-2288 |
| 2 | `nextLint` | Yes | Replace with Biome | 2289-2294 |
| 3 | `nextLintScript` | Yes | Update package.json lint script | 2295-2300 |
| 4 | `eslintConfig` | No | Suggest Biome alongside ESLint | 2301-2306 |
| 5 | `oldImageComponent` | Yes | next/legacy/image → next/image | 2307-2312 |
| 6 | `oldRouterImport` | Yes | next/router → next/navigation | 2313-2318 |
| 7 | `oldFontOptimization` | Yes | @next/font → next/font | 2319-2324 |
| 8 | `getServerSideProps` | No | Suggest Server Components | 2325-2330 |
| 9 | `unstableOptions` | No | Flag unstable_ APIs | 2331-2336 |
| 10 | `oldMetadataAPI` | Yes | Add Metadata type annotation | 2338-2343 |
| 11 | `missingMetadataImport` | Yes | Auto-add Metadata import | 2344-2360 |
| 12 | `oldSearchParams` | Yes | Add searchParams typing | 2361-2372 |
| 13 | `oldParams` | Yes | Add Promise<> params typing | 2373-2384 |
| 14 | `oldGenerateMetadata` | Yes | Add function type annotation | 2385-2390 |
| 15 | `missingGenerateMetadataImport` | Yes | Auto-add GenerateMetadataFunction import | 2391-2401 |

### Competitive Landscape (Web Research - December 2024):

**@next/codemod handles:**

| Pattern | @next/codemod | Layer 5 |
|---------|--------------|---------|
| @next/font → next/font | Yes (`next/15/next-font-imports`) | Yes |
| params/searchParams async | Yes (`next/15/async-request-apis`) | Partial (typing only) |
| next lint → ESLint CLI | Yes (`next/15/migrate-to-eslint-cli`) | Yes (→ Biome instead) |
| Metadata viewport split | Yes (`next/15/metadata-to-viewport-export`) | No |
| geo/ip removal | Yes (`next/15/replace-next-request-geo-ip`) | No |
| legacyBehavior removal | Not found | Yes |
| next/router → next/navigation | Not found in v15 codemods | Yes |
| getServerSideProps → RSC | Not found | Yes (warning only) |

**Official @next/codemod command:**
```bash
npx @next/codemod@canary upgrade latest
```

### What Makes Layer 5 POTENTIALLY Different:

1. **next lint → Biome** - Layer 5 recommends Biome, @next/codemod uses ESLint CLI
2. **legacyBehavior removal** - Not found in @next/codemod v15
3. **next/router → next/navigation** - Handles Pages → App Router migration
4. **Metadata type annotations** - Adds TypeScript types automatically

### How to Accurately Describe This:

**CORRECT:** "Layer 5 handles 15 Next.js 15.5 deprecation patterns, some overlapping with @next/codemod"

**CORRECT:** "Layer 5 migrates next lint to Biome (vs @next/codemod which migrates to ESLint CLI)"

**CORRECT:** "Layer 5 adds TypeScript type annotations for params and searchParams"

**INCORRECT:** "No other tool handles Next.js 15.5 deprecations" (@next/codemod exists)

**INCORRECT:** "First Next.js 15 migration tool" (@next/codemod is official)

### Limitations:

- Many patterns overlap with @next/codemod
- Regex-based, not AST-based like @next/codemod (which uses jscodeshift)
- Some patterns only add warnings, don't transform code

---

## Verified Claim #3: "ESLint to Biome Migration"

### TRUE - But Biome Has Official Migration Tool

**Actual Implementation (lines 1994-2274):**

```javascript
// Line 1996-2004
class BiomeMigrationTransformer {
  constructor() {
    this.configMappings = {
      'no-unused-vars': 'correctness/noUnusedVariables',
      'no-console': 'suspicious/noConsoleLog',
      'prefer-const': 'style/useConst',
      'no-var': 'style/noVar',
      'eqeqeq': 'suspicious/noDoubleEquals',
      'react/jsx-key': 'correctness/useJsxKeyInIterable',
      '@typescript-eslint/no-explicit-any': 'suspicious/noExplicitAny'
    };
  }
}
```

### Competitive Landscape (Web Research - December 2024):

| Tool | Auto-migrate | Rule mappings | Source |
|------|--------------|---------------|--------|
| **Biome CLI (official)** | Yes | Many (all supported ESLint rules) | `npx biome migrate eslint --write` |
| **Codemod.com** | Yes | Many | `npx codemod eslint/biome/migrate-rules` |
| **biome-interactive** | Yes (Go CLI) | Many | Community tool |
| **Next.js codemod** | Yes | N/A | `npx @next/codemod@latest biome` |
| **NeuroLint Layer 5** | Yes | 7 rules only | `BiomeMigrationTransformer` |

**Official Biome migration command:**
```bash
npx biome migrate eslint --write --include-inspired
npx biome migrate prettier --write
```

### What Layer 5 DOES NOT Have (vs Official Tools):

- Biome's official `migrate eslint` supports ALL ESLint rules
- Layer 5 only maps 7 specific rules
- Biome handles Prettier migration separately
- Biome handles plugin configurations (TypeScript ESLint, React, etc.)

### How to Accurately Describe This:

**CORRECT:** "Layer 5 includes basic ESLint → Biome migration with 7 rule mappings"

**CORRECT:** "For comprehensive ESLint → Biome migration, use `biome migrate eslint --write`"

**INCORRECT:** "Layer 5 is the only ESLint → Biome migration tool" (Biome has official CLI)

**INCORRECT:** "Complete ESLint migration" (only 7 rules mapped)

### Limitations:

- Only 7 ESLint rules mapped (Biome's official tool handles many more)
- Official `biome migrate eslint` is more comprehensive
- Should probably recommend using Biome's official tool instead

---

## Verified Claim #4: "Type-Safe Routing Transformer"

### TRUE - Generates TypeScript Interfaces for App Router

**Actual Implementation (lines 438-701):**

```javascript
// Line 438-480
class TypeSafeRoutingTransformer {
  constructor() {
    this.routePatterns = {
      page: /export\s+default\s+function\s+(\w+)\s*\(\s*\{[^}]*\}\s*\)/g,
      layout: /export\s+default\s+function\s+(\w+)\s*\(\s*\{[^}]*\}\s*\)/g,
      loading: /export\s+default\s+function\s+(\w+)\s*\(\s*\{[^}]*\}\s*\)/g,
      error: /export\s+default\s+function\s+(\w+)\s*\(\s*\{[^}]*\}\s*\)/g
    };
    
    this.routeFilePatterns = [
      'app/**/page.tsx',
      'app/**/page.ts',
      'app/**/layout.tsx',
      'app/**/layout.ts',
      'app/**/loading.tsx',
      'app/**/error.tsx',
      'app/**/not-found.tsx'
    ];
  }

  // Line 461-479
  extractRouteParams(filePath) {
    // Handles [param] and [...catchAll] patterns
    // Returns typed params object
  }

  // Line 516-524
  generateRouteTypes(filePath, routeParams) {
    return `interface ${interfaceName} {
  params: ${paramTypes};
  searchParams: { [key: string]: string | string[] | undefined };
}`;
  }
}
```

### Competitive Landscape (Web Research - December 2024):

| Tool | Type-safe routing | Source |
|------|------------------|--------|
| **next-safe-routes** | Yes (generates route types) | npm package |
| **nextjs-routes** | Yes (generates route types) | npm package |
| **@next/codemod** | No (handles async params, not typing) | Official |
| **NeuroLint Layer 5** | Yes (generates interface per route file) | `TypeSafeRoutingTransformer` |

### How to Accurately Describe This:

**CORRECT:** "Layer 5 generates TypeScript interfaces for Next.js App Router route files"

**CORRECT:** "Extracts route params from [slug] and [...catchAll] patterns"

**MORE ACCURATE:** "Similar functionality exists in next-safe-routes and nextjs-routes packages"

### Limitations:

- Similar packages exist (next-safe-routes, nextjs-routes)
- Regex-based extraction, not full TypeScript analysis
- May not handle all edge cases

---

## Verified Claim #5: "Smart 'use client' Directive Placement"

### TRUE - But @next/codemod Has Similar Feature

**Actual Implementation (lines 1432-1453):**

```javascript
// Line 1433-1436
const isReactFile = filePath && /\.(tsx?|jsx?)$/.test(filePath);
const hasClientHooks = /\buse(State|Effect|Ref|Callback|Memo|Context|Reducer|ImperativeHandle|LayoutEffect|DebugValue|Id|Transition|DeferredValue|SyncExternalStore|InsertionEffect)\b/.test(code);
const hasEventHandlers = /\bon[A-Z]\w+\s*=/.test(code); // onClick, onChange, etc.
const needsUseClient = isReactFile && (hasClientHooks || hasEventHandlers);

if (needsUseClient) {
  const hasUseClient = /['"]use client['"];?/.test(code);
  let withoutDirectives = code.replace(/^['"]use client['"];?\s*/m, '');
  if (!hasUseClient || withoutDirectives !== code) {
    changes.push({ description: "Placed 'use client' directive at top", location: { line: 1 } });
    code = `'use client';\n` + withoutDirectives;
  }
}
```

### Competitive Landscape (Web Research - December 2024):

| Tool | Auto-add 'use client' | Auto-Fix | Detection method | Source |
|------|----------------------|----------|------------------|--------|
| **next/13/upsert-use-client-directive** | Yes | Yes (codemod) | Hooks + event handlers + browser APIs | `npx @next/codemod@latest next/13/upsert-use-client-directive` |
| **eslint-plugin-use-client (NaverPay)** | Yes | **Yes (--fix)** | React hooks + browser APIs + event handlers | https://github.com/NaverPayDev/eslint-plugin-use-client |
| **eslint-plugin-nextjs-enforce-use-client** | Yes | **Yes (--fix)** | React hooks + MUI + configurable | https://github.com/rkristelijn/eslint-plugin-nextjs-enforce-use-client |
| **Biome (noNextAsyncClientComponent)** | No (prevents async) | No | Checks async client components | https://biomejs.dev/linter/rules/no-next-async-client-component/ |
| **NeuroLint Layer 5** | Yes | Yes | 15 hooks + event handlers | `fix-layer-5-nextjs.js` lines 1432-1453 |

**Official @next/codemod command:**
```bash
npx @next/codemod@latest next/13/upsert-use-client-directive ./app
```

**ESLint plugin with auto-fix:**
```bash
npm install eslint-plugin-use-client --save-dev
npx eslint --fix .
```

### What Layer 5 Detects (15 hooks):

| Category | Hooks |
|----------|-------|
| Core State | `useState`, `useReducer` |
| Effects | `useEffect`, `useLayoutEffect`, `useInsertionEffect` |
| Refs | `useRef`, `useImperativeHandle` |
| Performance | `useMemo`, `useCallback` |
| Context | `useContext` |
| Debug | `useDebugValue` |
| React 18+ | `useId`, `useTransition`, `useDeferredValue`, `useSyncExternalStore` |

### How to Accurately Describe This:

**CORRECT:** "Layer 5 detects 15 React hooks that require 'use client'"

**CORRECT:** "Similar functionality exists in @next/codemod and ESLint plugins"

**INCORRECT:** "No other tool auto-adds 'use client'" - Multiple tools do this:
- `@next/codemod` (next/13/upsert-use-client-directive)
- `eslint-plugin-use-client` (NaverPay) with --fix
- `eslint-plugin-nextjs-enforce-use-client` with --fix

### Limitations:

- Multiple tools now have 'use client' auto-add with auto-fix
- Regex-based detection may have edge cases
- Doesn't analyze whether hooks are actually called (just presence in code)

---

## Verified Claim #6: "Centralized Backup System"

### TRUE - Integrates with NeuroLint BackupManager

**Actual Implementation (lines 1034-1051):**

```javascript
// Line 1034-1051 (in transformRouteFile)
const backupManager = new BackupManager({ 
  backupDir: '.neurolint-backups', 
  maxBackups: 10 
});
const backupResult = await backupManager.createBackup(filePath, 'layer-5-nextjs-route-transform');
if (backupResult.success) {
  backupPath = backupResult.backupPath;
  await fs.writeFile(filePath, transformation.code);
  if (verbose) {
    process.stdout.write(`[SUCCESS] Transformed ${path.basename(filePath)}\n`);
    process.stdout.write(`[INFO] Centralized backup: ${path.basename(backupPath)}\n`);
  }
}
```

### Competitive Landscape:

| Tool | Creates backups | Centralized location |
|------|-----------------|---------------------|
| **react-codemod** | No (relies on git) | N/A |
| **@next/codemod** | No (relies on git) | N/A |
| **Biome** | No | N/A |
| **NeuroLint Layer 5** | Yes | `.neurolint-backups/` |

### How to Accurately Describe This:

**CORRECT:** "Layer 5 creates centralized backups in .neurolint-backups before transformations"

**CORRECT:** "Unlike react-codemod and @next/codemod, Layer 5 has built-in backup before changes"

**This IS a genuine differentiator** - Most codemods rely on git for rollback.

---

## What NOT to Claim

| Claim | Why It's Wrong |
|-------|----------------|
| "No other tool does React 19 migration" | react-codemod is official and comprehensive |
| "First Next.js 15 migration tool" | @next/codemod is official and predates |
| "Only ESLint → Biome migration" | Biome has official `migrate eslint` CLI |
| "Only 'use client' auto-add tool" | @next/codemod + eslint-plugin-use-client + eslint-plugin-nextjs-enforce-use-client all do this |
| "AST-based transformation" | Uses regex + manual parenthesis parsing |
| "Complete type-safe routing" | Similar packages exist (next-safe-routes, nextjs-routes) |

---

## Defensible Differentiators

Based on verified competitive research, these are ACTUALLY unique:

1. **"All-in-one tool"** - Combines React 19 + Next.js 15.5 + Biome in single package
   - Other tools: react-codemod (React only), @next/codemod (Next.js only), biome migrate (Biome only)

2. **"Centralized backup before every transformation"** - TRUE and unique
   - Other codemods rely on git for rollback

3. **"next lint → Biome specifically"** - TRUE
   - @next/codemod migrates to ESLint CLI, not Biome

4. **"Unique root variable naming"** - TRUE (need to verify if react-codemod does this)
   - Generates `root`, `root1`, `root2` for multiple render calls

5. **"Integrated with NeuroLint layer system"** - TRUE
   - Works with Layers 6, 7, 8 for comprehensive migration

---

## Honest Competitive Summary

| Feature | Official Tool | Layer 5 | Differentiator? |
|---------|--------------|---------|-----------------|
| ReactDOM.render → createRoot | react-codemod | Yes | **No** (similar) |
| ReactDOM.hydrate → hydrateRoot | react-codemod | Yes | **No** (similar) |
| act import migration | react-codemod | Yes | **No** (similar) |
| @next/font → next/font | @next/codemod | Yes | **No** (similar) |
| params → Promise<> | @next/codemod | Partial | **No** (partial) |
| ESLint → Biome | biome CLI | 7 rules only | **No** (weaker) |
| 'use client' auto-add | @next/codemod, eslint-plugin-use-client | Yes | **No** (similar) |
| **Centralized backups** | None | Yes | **YES** |
| **All-in-one package** | Separate tools | Yes | **YES** |
| **next lint → Biome** | → ESLint CLI | → Biome | **YES** |
| **Layer integration** | None | Yes | **YES** |

---

## Sample X Post Templates (Honest Version)

### Thread 1: "Why I Built Layer 5 (Not Replacing react-codemod)"

```
1/ I built a React 19 + Next.js 15.5 migration tool.

But I'm not going to lie: react-codemod and @next/codemod exist and are great.

Here's why Layer 5 is different anyway: [thread]

2/ The problem with using 4 different tools:

- react-codemod for React 19 DOM APIs
- @next/codemod for Next.js 15
- biome migrate for ESLint → Biome
- Manual work for edge cases

Each has different syntax, different options.

3/ Layer 5 combines all of them:

- React 19 DOM APIs (render, hydrate, act)
- Next.js 15.5 deprecations (15 patterns)
- Biome migration
- Type-safe routing

One command: npx @neurolint/cli fix ./src --layer=5

4/ What Layer 5 does that others don't:

- Centralized backups in .neurolint-backups/
- next lint → Biome (not ESLint CLI)
- Works with Layers 6, 7, 8 for full migration

5/ Be honest about your tools.

react-codemod handles ReactDOM.render.
@next/codemod handles @next/font.
biome migrate handles ESLint rules.

Layer 5 combines them + adds features.

That's the real value.

github.com/Alcatecablee/Neurolint
```

### Thread 2: "Centralized Backups (Actually Unique)"

```
1/ Most codemods don't create backups.

They assume you're using git.

Layer 5 creates centralized backups before EVERY transformation: [thread]

2/ react-codemod approach:
"Make sure to commit your changes before running"

This is fine. But what if:
- You're iterating quickly
- You forget to commit
- You want to compare before/after

3/ Layer 5 approach:

.neurolint-backups/
├── page.tsx.layer-5-nextjs.1702345678901.bak
├── layout.tsx.layer-5-nextjs.1702345678902.bak
└── ...

Maximum 10 backups per context.
Always recoverable.

4/ It's a small thing. But it adds confidence.

Run Layer 5, check the results, restore if needed.

No git commit required between iterations.

5/ Try it:

npx @neurolint/cli fix ./src --layer=5

Backups in .neurolint-backups/

github.com/Alcatecablee/Neurolint
```

---

## Verification Commands

Anyone can verify these claims by running:

```bash
# Count total lines (expect: 2804)
wc -l scripts/fix-layer-5-nextjs.js

# Find deprecation patterns class (expect: line 2280)
grep -n "class NextJS15DeprecationHandler" scripts/fix-layer-5-nextjs.js

# Count deprecation pattern keys (expect: 15)
grep -n "pattern:" scripts/fix-layer-5-nextjs.js | wc -l

# Find 'use client' detection logic
grep -n "hasClientHooks" scripts/fix-layer-5-nextjs.js

# Verify backup integration
grep -n "BackupManager" scripts/fix-layer-5-nextjs.js

# Verify Biome rule mappings (expect: 7)
grep -n "configMappings" scripts/fix-layer-5-nextjs.js -A 10
```

---

## File References for Verification

| File | Lines | What It Contains |
|------|-------|------------------|
| `fix-layer-5-nextjs.js` | 33-131 | ReactDOM.render conversion |
| `fix-layer-5-nextjs.js` | 137-231 | ReactDOM.hydrate conversion |
| `fix-layer-5-nextjs.js` | 237-261 | unmountComponentAtNode warning |
| `fix-layer-5-nextjs.js` | 267-293 | findDOMNode deprecation warning |
| `fix-layer-5-nextjs.js` | 299-331 | react-dom/test-utils migration |
| `fix-layer-5-nextjs.js` | 438-701 | TypeSafeRoutingTransformer class |
| `fix-layer-5-nextjs.js` | 1432-1453 | 'use client' directive logic |
| `fix-layer-5-nextjs.js` | 1994-2274 | BiomeMigrationTransformer class |
| `fix-layer-5-nextjs.js` | 2280-2402 | NextJS15DeprecationHandler class |
| `fix-layer-5-nextjs.js` | 2795-2805 | module.exports |

---

*Document created: December 11, 2025*
*Verified against: NeuroLint codebase + competitive web research*
*Layer 5 file: scripts/fix-layer-5-nextjs.js (2804 lines)*

**Competitive tools verified:**
- react-codemod: https://github.com/reactjs/react-codemod
- @next/codemod: https://nextjs.org/docs/app/guides/upgrading/codemods
- biome migrate: https://biomejs.dev/guides/migrate-eslint-prettier/
- next/13/upsert-use-client-directive: https://app.codemod.com/registry/next/13/upsert-use-client-directive
- eslint-plugin-use-client (NaverPay): https://github.com/NaverPayDev/eslint-plugin-use-client
- eslint-plugin-nextjs-enforce-use-client: https://github.com/rkristelijn/eslint-plugin-nextjs-enforce-use-client
- Biome noNextAsyncClientComponent: https://biomejs.dev/linter/rules/no-next-async-client-component/
